import { configs } from '../src/service/configService';
import { expect } from 'chai';

describe('Service config tests', () => {

    it('return config database', () => {


    });

    expect().to.equal();
});